# HiddenNotch 기획안

## 1. 프로젝트 개요

**HiddenNotch**는 MacBook의 노치가 보이지 않도록 메뉴바 배경을 항상 검은색으로 유지하는 초경량 macOS 메뉴바 앱이다.

기능을 많이 넣는 앱이 아니라, 다음 한 가지 문제를 확실하게 해결하는 것이 목표다.

> 외부 모니터 연결·해제, 배경화면 변경, 잠자기 복귀, 재부팅 이후에도 별도 조작 없이 노치 숨김 상태가 유지되어야 한다.

앱은 Dock에 나타나지 않고 macOS 메뉴바에서만 실행된다.

---

## 2. 이름

- 제품명: **HiddenNotch**
- 표시명 후보: `HiddenNotch` 또는 `Hidden Notch`
- Xcode 프로젝트명: `HiddenNotch`
- Bundle Identifier 예시: `com.rrllab.HiddenNotch`

`HiddenNotch`는 기능을 바로 이해할 수 있고 개인용 유틸리티 이름으로 적합하다. 다만 외부 공개나 앱스토어 출시 전에는 상표, 앱스토어, GitHub 이름 중복 여부를 다시 확인한다.

---

## 3. 핵심 목표

### 필수 목표

1. 노치가 있는 MacBook 화면의 메뉴바 배경을 완전한 검은색으로 유지한다.
2. Dock 아이콘을 표시하지 않는다.
3. 메뉴바 아이콘에서 켜기·끄기만 할 수 있다.
4. Mac 로그인 시 자동으로 실행할 수 있다.
5. 외부 모니터를 연결하거나 분리해도 자동으로 정상 상태를 복구한다.
6. 잠자기와 화면 꺼짐 이후 복귀해도 자동으로 다시 적용한다.
7. 배경화면을 바꿔도 효과가 유지된다.
8. 디스플레이 해상도, 배율, 배열이 바뀌어도 자동으로 다시 계산한다.
9. 앱을 삭제했다 다시 설치할 필요 없이 자체 복구한다.

### 비목표

다음 기능은 만들지 않는다.

- 색상 선택
- 투명도 조절
- 그라데이션
- 노치 주변 애니메이션
- 음악 플레이어
- 파일 보관함
- Dynamic Island 기능
- 계정, 서버, 동기화
- 사용 통계 및 분석
- 자동 업데이트 시스템
- 복잡한 설정 창

---

## 4. 사용자 경험

### 최초 실행

1. 앱을 실행한다.
2. HiddenNotch가 노치 디스플레이를 자동 감지한다.
3. 메뉴바 상단이 즉시 검은색으로 적용된다.
4. 메뉴바 오른쪽에 HiddenNotch 아이콘이 표시된다.
5. Dock에는 앱이 표시되지 않는다.

### 메뉴바 메뉴

```text
HiddenNotch
──────────────
✓ 노치 숨기기
✓ 로그인 시 자동 실행
다시 적용
──────────────
HiddenNotch 종료
```

별도의 설정 창은 만들지 않는다.

### 기본값

- 노치 숨기기: 켜짐
- 로그인 시 자동 실행: 켜짐
- 외부 모니터: 노치가 없는 화면에는 적용하지 않음
- 네트워크 사용: 없음

---

## 5. 권장 기술 구성

### 개발 환경

- 언어: Swift
- UI: AppKit 중심, 필요한 부분만 SwiftUI 사용
- 앱 유형: macOS Menu Bar App
- 최소 지원 버전: macOS 13 Ventura 이상
- 아키텍처: 단일 네이티브 앱
- 외부 라이브러리: 사용하지 않음

Electron으로 만들지 않는다. 이 앱은 시스템 디스플레이와 창 상태를 직접 다뤄야 하므로 Swift/AppKit이 가장 적합하다.

### Dock에서 숨기기

다음 방식으로 일반 앱이 아닌 메뉴바 전용 앱으로 동작시킨다.

- `Info.plist`의 `LSUIElement = true`
- 앱 시작 시 activation policy를 `.accessory`로 설정
- `NSStatusItem`으로 메뉴바 아이콘 생성

---

## 6. 핵심 구현 전략

## 6.1 배경화면을 직접 수정하지 않는 방식 우선

HiddenNotch는 가능하면 원본 배경화면 파일을 편집하거나 복제하지 않는다.

대신 노치가 있는 화면의 상단 메뉴바 영역 뒤에 검은색 오버레이 창을 유지한다.

이 방식의 장점은 다음과 같다.

- 배경화면을 바꿔도 다시 이미지를 생성할 필요가 없다.
- 동적 배경화면을 유지할 수 있다.
- 외부 모니터 연결 시 배경화면 경로가 바뀌어도 영향을 받지 않는다.
- 원본 배경화면 파일을 건드리지 않는다.
- 캐시된 배경화면 파일이 꼬이는 문제를 피할 수 있다.

단, 개발 초기에 실제 macOS 메뉴바 레이어 아래에서 검은색이 정확히 표현되는지 기술 검증을 먼저 한다.

### 기술 검증 통과 기준

- 메뉴바 아이콘과 앱 메뉴 글자가 정상적으로 보인다.
- 검은색 영역이 마우스 클릭을 가로채지 않는다.
- 여러 Space에서 유지된다.
- 전체화면 앱에서 화면 상단 콘텐츠를 불필요하게 덮지 않는다.
- 내장 디스플레이와 외부 디스플레이를 구분한다.

오버레이 방식이 특정 macOS 버전에서 정확히 작동하지 않을 때만 배경화면 합성 방식을 예비 대안으로 검토한다.

---

## 6.2 노치 화면 감지

각 `NSScreen`을 검사하여 다음 조건으로 노치가 있는 디스플레이를 판정한다.

- `safeAreaInsets.top > 0`
- 또는 `auxiliaryTopLeftArea`, `auxiliaryTopRightArea`가 존재
- 화면 이름만으로 판정하지 않음
- 메인 디스플레이 여부만으로 판정하지 않음
- 저장해 둔 화면 번호만 믿지 않음

외부 모니터가 메인 화면으로 바뀌더라도 내장 MacBook 화면의 노치 적용이 유지되어야 한다.

---

## 6.3 화면별 오버레이

노치가 감지된 각 화면마다 하나의 `NSPanel` 또는 borderless `NSWindow`를 만든다.

필수 속성:

- 배경색: 순수 검정 `#000000`
- 테두리 없음
- 그림자 없음
- 제목 표시줄 없음
- 클릭 무시: `ignoresMouseEvents = true`
- 키보드 포커스를 받지 않음
- 모든 Space에 참여
- 화면 캡처와 일반 창 전환에 불필요하게 나타나지 않도록 설정
- 메뉴바 아이콘과 시스템 메뉴 클릭을 방해하지 않음

오버레이 높이는 하드코딩하지 않고 해당 화면의 실제 메뉴바 및 safe area 값을 기준으로 계산한다.

---

## 7. 안정성 설계

HiddenNotch의 핵심은 기능 수가 아니라 **상태가 풀렸을 때 자동으로 다시 만드는 것**이다.

## 7.1 재구성 원칙

디스플레이 상태가 변하면 기존 오버레이 위치만 수정하지 않는다.

다음 순서로 완전히 재구성한다.

1. 현재 오버레이 전부 제거
2. `NSScreen.screens` 다시 조회
3. 현재 노치 화면 다시 감지
4. 새 화면 좌표와 배율 계산
5. 화면별 오버레이 새로 생성
6. 정상 생성 여부 확인

이전 디스플레이 ID나 화면 순서를 장기간 저장하지 않는다.

---

## 7.2 감지해야 할 이벤트

다음 이벤트가 발생하면 `rebuildOverlays()`를 실행한다.

- 앱 최초 실행
- 외부 모니터 연결
- 외부 모니터 분리
- 미러링과 확장 모드 전환
- 디스플레이 배열 변경
- 해상도 또는 배율 변경
- 화면 회전
- Mac 잠자기 복귀
- 디스플레이 절전 복귀
- 사용자 세션 재활성화
- 앱이 다시 활성화됨
- 수동으로 `다시 적용` 선택
- 시스템 설정에서 디스플레이 구성이 바뀜

대표적으로 다음 시스템 알림을 사용한다.

- `NSApplication.didChangeScreenParametersNotification`
- `NSWorkspace.didWakeNotification`
- `NSWorkspace.screensDidWakeNotification`
- `NSWorkspace.sessionDidBecomeActiveNotification`

---

## 7.3 지연 재시도

외부 모니터 연결 직후에는 macOS가 화면 목록과 좌표를 한 번에 확정하지 않을 수 있다.

따라서 디스플레이 변경 이벤트가 오면 한 번만 적용하지 않고 다음과 같이 재검사한다.

```text
즉시 재구성
0.5초 후 재확인
1.5초 후 재확인
3초 후 최종 재확인
```

연속 이벤트는 debounce하여 불필요한 창 생성을 줄인다.

---

## 7.4 상태 감시 Watchdog

이벤트 알림을 놓치는 경우를 대비해 가벼운 상태 점검을 수행한다.

- 주기: 15초 또는 30초
- CPU 사용이 거의 없도록 단순 비교만 수행
- 확인 항목:
  - 현재 노치 화면 수
  - 현재 오버레이 수
  - 오버레이가 연결된 화면
  - 오버레이 프레임과 화면 프레임 일치 여부
  - 오버레이가 화면에 존재하는지 여부

불일치가 있으면 자동으로 `rebuildOverlays()`를 실행한다.

이 Watchdog이 TopNotch와 같은 “한 번 풀리면 재설치해야 하는 상태”를 방지하는 핵심 안전장치다.

---

## 7.5 앱 시작 시 초기화

앱이 시작될 때 이전 실행 상태를 복원하려 하지 않는다.

매번 다음과 같이 시작한다.

1. 기존 저장값 중 화면 관련 캐시는 무시
2. 현재 연결된 화면을 새로 검색
3. 현재 좌표 기준으로 오버레이 생성
4. 적용 결과 확인
5. 실패하면 짧은 간격으로 최대 3회 재시도

화면 식별자, 좌표, 외부 모니터 순서를 설정 파일에 고정 저장하지 않는다.

---

## 8. 로그인 시 자동 실행

macOS 13 이상에서는 `SMAppService`를 사용한다.

메뉴의 `로그인 시 자동 실행`을 켜면:

- 로그인 항목에 HiddenNotch 등록
- 다음 로그인부터 백그라운드 실행
- 실행 직후 현재 디스플레이를 다시 감지
- Dock 표시 없이 메뉴바 아이콘만 생성

등록 상태가 macOS에서 사용자 승인을 요구하는 경우 메뉴에서 상태를 안내하고 시스템 설정을 열 수 있게 한다.

---

## 9. 데이터 저장

`UserDefaults`에는 다음 두 값만 저장한다.

```text
isEnabled: Bool
launchAtLoginRequested: Bool
```

다음 값은 저장하지 않는다.

- 화면 ID
- 화면 순서
- 화면 해상도
- 화면 좌표
- 배경화면 경로
- 오버레이 프레임
- 외부 모니터 이름

화면 관련 값은 매 실행과 매 이벤트마다 새로 계산한다.

---

## 10. 파일 구조 제안

```text
HiddenNotch/
├── HiddenNotch.xcodeproj
├── HiddenNotch/
│   ├── App/
│   │   ├── HiddenNotchApp.swift
│   │   └── AppDelegate.swift
│   ├── StatusBar/
│   │   ├── StatusBarController.swift
│   │   └── StatusMenuBuilder.swift
│   ├── Display/
│   │   ├── DisplayCoordinator.swift
│   │   ├── NotchDetector.swift
│   │   └── ScreenSnapshot.swift
│   ├── Overlay/
│   │   ├── OverlayController.swift
│   │   ├── OverlayPanel.swift
│   │   └── OverlayGeometry.swift
│   ├── Stability/
│   │   ├── SystemEventMonitor.swift
│   │   ├── OverlayWatchdog.swift
│   │   └── RebuildScheduler.swift
│   ├── LoginItem/
│   │   └── LoginItemManager.swift
│   ├── Settings/
│   │   └── AppSettings.swift
│   ├── Resources/
│   │   └── Assets.xcassets
│   └── Info.plist
├── Tests/
│   ├── NotchDetectorTests.swift
│   ├── OverlayGeometryTests.swift
│   └── RebuildSchedulerTests.swift
└── README.md
```

---

## 11. 주요 컴포넌트

### `AppDelegate`

- 앱 시작 처리
- Dock 비표시 정책 적용
- 상태바 생성
- 시스템 이벤트 감시 시작
- 최초 오버레이 생성

### `DisplayCoordinator`

- 현재 화면 목록 조회
- 노치 디스플레이 감지
- 오버레이 전체 재구성
- 현재 상태 스냅샷 관리

### `NotchDetector`

- `NSScreen.safeAreaInsets`
- 상단 auxiliary area
- 노치 여부 판정

### `OverlayController`

- 화면별 오버레이 생성 및 제거
- 창 위치와 크기 계산
- 창 레벨 및 Space 동작 관리

### `SystemEventMonitor`

- 모니터 연결·해제
- 잠자기·복귀
- 세션 활성화
- 화면 설정 변경 감지

### `RebuildScheduler`

- 연속 이벤트 debounce
- 0.5초, 1.5초, 3초 재시도
- 동시에 여러 rebuild가 실행되지 않게 직렬화

### `OverlayWatchdog`

- 주기적인 무결성 확인
- 이상 발견 시 자동 재구성

### `LoginItemManager`

- `SMAppService` 등록
- 등록 해제
- 승인 필요 상태 확인

---

## 12. 테스트 시나리오

### 기본

- 앱 실행 직후 노치가 검게 숨겨진다.
- Dock에 앱이 나타나지 않는다.
- 메뉴바에서 켜기·끄기가 된다.
- 메뉴바 아이콘을 종료하면 오버레이가 제거된다.

### 외부 모니터

- 앱 실행 전에 외부 모니터 연결
- 앱 실행 후 외부 모니터 연결
- 외부 모니터 분리
- 다른 USB-C 포트로 재연결
- HDMI 허브를 통한 연결
- 미러링 모드 전환
- 확장 모드 전환
- 외부 모니터를 주 디스플레이로 변경
- MacBook 덮개를 닫고 클램셸 모드 사용
- 클램셸 모드에서 다시 덮개 열기

### 화면 설정

- 해상도 변경
- 배율 변경
- 디스플레이 배열 변경
- 화면 회전
- 별도 Space 사용
- 여러 데스크톱 이동
- 전체화면 앱 진입과 해제

### 시스템 상태

- 화면만 잠자기
- Mac 전체 잠자기
- 잠자기 중 모니터 분리
- 잠자기 중 모니터 연결
- 사용자 로그아웃과 로그인
- Mac 재부팅
- macOS 업데이트 후 첫 실행

### 배경화면

- 일반 이미지 변경
- 밝은 배경화면으로 변경
- 어두운 배경화면으로 변경
- 동적 배경화면 변경
- Space별 배경화면 변경

---

## 13. 완료 기준

다음 조건을 모두 만족하면 1.0으로 본다.

1. 외부 모니터를 20회 이상 반복 연결·분리해도 재실행이 필요하지 않다.
2. 잠자기·복귀 10회 후에도 자동 적용된다.
3. 배경화면을 변경해도 검은 메뉴바가 유지된다.
4. 해상도와 화면 배열을 바꿔도 3초 이내 복구된다.
5. Mac 재부팅 후 사용자가 앱을 직접 열지 않아도 실행된다.
6. Dock에 HiddenNotch가 나타나지 않는다.
7. 노치가 없는 외부 모니터에는 검은 띠를 만들지 않는다.
8. 메뉴바 클릭과 아이콘 조작을 방해하지 않는다.
9. 평상시 CPU 사용량이 사실상 0%에 가깝다.
10. 네트워크 연결 없이 모든 기능이 작동한다.

---

## 14. 개발 순서

### Phase 0 — 기술 검증

- 노치 화면 감지
- 검은 오버레이 1개 생성
- 메뉴바 아이콘과 클릭이 정상인지 확인
- 전체화면과 Space 동작 확인

이 단계에서 오버레이 방식이 정확한 검은색을 만들 수 있는지 먼저 판정한다.

### Phase 1 — 최소 기능

- 메뉴바 전용 앱
- 노치 숨기기 켜기·끄기
- 노치 화면 자동 감지
- 화면별 오버레이 생성
- 앱 종료

### Phase 2 — 안정성

- 외부 모니터 이벤트 감지
- 잠자기와 복귀 감지
- debounce와 지연 재시도
- Watchdog
- 수동 `다시 적용`

### Phase 3 — 자동 실행

- 로그인 시 자동 실행
- 등록 상태 표시
- 재부팅 테스트

### Phase 4 — 실사용 검증

- 외부 모니터 반복 연결
- 배경화면 변경
- 클램셸 모드
- 전체화면과 Space
- 장시간 실행

---

## 15. 구현 원칙

- 기능 추가보다 안정성을 우선한다.
- 디스플레이 상태를 캐시하지 말고 매번 다시 조회한다.
- 단일 이벤트 한 번만 믿지 말고 지연 재검사를 한다.
- 이벤트를 놓칠 가능성에 대비해 Watchdog을 둔다.
- 화면 변경 시 부분 수정보다 전체 재구성을 우선한다.
- private API를 사용하지 않는다.
- 외부 패키지를 추가하지 않는다.
- 실패하더라도 앱 재설치 없이 `다시 적용` 또는 자동 복구가 가능해야 한다.
- 사용자가 평소 앱의 존재를 신경 쓰지 않아도 되는 상태를 목표로 한다.

---

## 16. 최종 제품 정의

HiddenNotch는 다양한 기능을 제공하는 노치 유틸리티가 아니다.

> MacBook의 메뉴바를 검은색으로 유지하고, 어떤 디스플레이 변화가 생겨도 스스로 다시 적용하는 앱이다.

버튼은 적고 설정은 단순해야 한다. 대신 외부 모니터 연결, 배경화면 변경, 잠자기 복귀, 재부팅 같은 실제 사용 환경에서 풀리지 않는 것이 제품의 핵심 가치다.
